# Mison Gap Fixes Implementation Plan

**Goal:** spec violation fixes (Connect schema guard bypass, option-edit loss, flat error classification) + 4 minor gaps

**Architecture:** all changes within existing layers. `env` gains sentinel error + `Tool.Options`; `Engine.Connect` gains parse-before-reset; flows gain fatal/warn split. No new files.

**Scope:** mise.lock §8 (DESIGN.md) is a separate milestone — NOT in this plan.

## Task 1: ErrFutureSchema sentinel
- `internal/env/schema.go`: add `var ErrFutureSchema = errors.New("declaration uses a newer schema")`; checkSchema wraps it (`%w: schema %d, this mison supports %d — upgrade mison`).
- Test: `TestFutureSchemaErrorIsSentinel` — errors.Is + "upgrade mison" message kept.

## Task 2: Connect() schema guard
- `internal/usecase/sync.go` Connect: after RemoteIsEmpty check, before ResetHard:
  `if _, err := e.configAt("origin/main"); err != nil { return err }`
- Test: `TestConnectRejectsFutureSchemaRemote` — future-schema remote, local dir content must survive Connect error.
- BEHAVIOR.md: init edge row.

## Task 3: fatal vs warn classification
- `commitAndPush(message, policy) error`: ErrFutureSchema → UI.Fail + return err; other → Warn defer + nil. Callers propagate.
- RunSync SmartPull error: ErrFutureSchema → return err, else warn-continue.
- Tests: `TestInstallFutureSchemaPushIsFatal`, `TestSyncFutureSchemaPullIsFatal` (fakeRepo pushErr/pullErr).
- BEHAVIOR.md rows.

## Task 4: Options-aware merge
- `Tool.Options map[string]any` (nil = bare string). toTool populates (non-version/os keys). toolEqual adds reflect.DeepEqual on Options.
- SetTool: non-nil Options → exact replace; nil → preserve (current behavior).
- Tests: `TestMergeOptionOnlyRemoteEditTaken`, `TestMergeOptionBothChangedDifferentlyConflicts`, `TestSetToolNilOptionsPreservesExisting`, `TestSetToolExactOptionsReplaces`.
- BEHAVIOR.md env fine-edge rows.

## Task 5: resolveRepoName explicit flag wins
- `if flagged != "" { return flagged }` at top; fallback read persisted; default DefaultRepoName.
- Test: persisted "other-env" + flag "mison-env" → flag used + persisted.

## Task 6: verification warns on list error
- verifyVisible/verifyDeclaredApplied: ListInstalled err → UI.Warn("could not verify installation (...)") instead of silent return.
- Test: fakeMise list error → warn call.

## Task 7: prune continues past failures
- Shared pruneAll helper: attempt every orphan, collect failures, `fmt.Errorf("pruned with failures: %s", ...)`.
- Test: orphan a fails, orphan b still removed, error returned.

## Task 8: init create-race fallback
- Extract `connectExisting(repo, repoName)`. CreatePrivateRepo error + RepoExists → connectExisting.
- Test: fakeGh create error + exists true → RunInit succeeds via Connect.

## Task 9: final
- `make lint && make test && make build`; BEHAVIOR.md known-gaps table updated.
